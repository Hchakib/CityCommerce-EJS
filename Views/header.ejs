<header class="header">
    <div class="header-content">
        <img src="/image/img.png" class="header_logo" alt="ShelbyFripe Logo">
        <span class="FashionSphere">ShelbyFripe</span>
        <nav class="navbar">
            <a href="/Home"><i class="fas fa-home"></i> Home</a>
            <div class="dropdown">
                <a href="#"><i class="fas fa-box-open"></i> Product</a>
                <div class="dropdown-content">
                    <a href="/mens">Men's Wear</a>
                    <a href="/womens">Women's Wear</a>
                    <a href="/accessories">Accessories</a>
                </div>
            </div>
            <a href="/your_products"><i class="fas fa-box-open"></i> Your Products</a>
            <a href="/add_product"><i class="fas fa-box-open"></i> Add Product</a>
            <a href="/cart"><i class="fas fa-shopping-cart"></i> Cart</a>
            <a href="/login"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </nav>
    </div>
</header>

<style>
    .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 30px;
    background-color: #7e5a3c; /* Bois moyen (chêne) */
    border-bottom: 3px solid #5e432f; /* Bordure bois sombre (noix) */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.header-content {
    display: flex;
    align-items: center;
    width: 100%;
}

.header_logo {
    height: 60px;
    width: auto;
    margin-right: 20px;
    border: 2px solid #5e432f;
    border-radius: 50%;
}

.FashionSphere {
    font-size: 32px;
    font-weight: bold;
    color: #e4d1a1; /* Jaune bois naturel */
    margin-right: auto;
    font-family: 'Georgia', serif;
    text-transform: uppercase;
    letter-spacing: 3px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
}

.navbar {
    display: flex;
    gap: 20px; /* Réduit l'écart entre les éléments */
    margin-left: auto;
    align-items: center;
}

.navbar a {
    text-decoration: none;
    color: #e4d1a1; /* Jaune bois naturel */
    font-size: 14px;
    font-family: 'Courier New', Courier, monospace;
    border: 2px solid #e4d1a1;
    border-radius: 20px;
    padding: 6px 10px;
    transition: background-color 0.3s ease, transform 0.3s ease;
    display: flex;
    align-items: center;
}

.navbar a:hover {
    background-color: #e4d1a1;
    color: #7e5a3c; /* Bois moyen (chêne foncé) */
    transform: scale(1.1);
}

/* Dropdown styles */
.dropdown {
    position: relative;
}

.dropdown-content {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    background-color: #7e5a3c;
    border: 2px solid #5e432f;
    border-radius: 10px;
    z-index: 1000;
    min-width: 150px;
    padding: 10px;
}

.dropdown-content a {
    display: block;
    padding: 10px;
    color: #e4d1a1;
    text-decoration: none;
    font-size: 16px;
    font-family: 'Courier New', Courier, monospace;
}

.dropdown-content a:hover {
    background-color: #e4d1a1;
    color: #7e5a3c;
}

.dropdown:hover .dropdown-content {
    display: block;
}

</style>